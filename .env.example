# สำหรับรัน local (npm run dev / seed / mock) — คัดลอกเป็น .env ถ้าต้องการ
MONGO_URI=mongodb://localhost:27017
DB_NAME=akr
MQTT_URL=mqtt://localhost:1883
JWT_SECRET=dev-secret-change-me
TZ=Asia/Bangkok
